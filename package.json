{
  "name": "yeti-arena-sdk",
  "version": "0.0.0",
  "private": true,
  "description": "SDK and scaffolder for joining the YetiFi trading arena",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
